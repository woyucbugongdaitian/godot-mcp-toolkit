# Contributing

1. Create an issue before starting non-trivial work.
2. Keep tools small, explicit, and grouped by domain.
3. Add or update a regression test for server behavior changes.
4. Run `npm.cmd run check`, `npm.cmd test`, and `npm.cmd run build` before opening a pull request.
5. Do not change compatibility-sensitive environment variables or addon paths without documenting a migration path.
